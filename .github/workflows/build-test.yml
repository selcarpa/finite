name: build-test

on:
  push:
    branches:
      - '**'
    tags-ignore:
      - '**'
jobs:
  build-test:
    runs-on: ubuntu-20.04
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
    steps:
      - uses: actions/checkout@v4
        name: Checkout

      - name: Print tree
        shell: bash
        run: tree .

      - name: Prepare environments
        run: sudo apt update -y && sudo apt install wget -y && wget https://github.com/gohugoio/hugo/releases/download/v0.120.4/hugo_0.120.4_linux-amd64.deb && sudo apt install ./hugo*.deb -y

      - name: Build with Hugo
        shell: bash
        run: hugo
        
      - name: Print tree2
        shell: bash
        run: tree .

      - uses: actions/upload-artifact@v3
        name: Store the build
        with:
          name: public
          path: public/

